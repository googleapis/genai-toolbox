sources:
  redis-source:
    kind: redis
    address:
      - 127.0.0.1:6379
    useGCPIAM: true
    clusterEnabled: false

tools:
  redis-tool:
    kind: redis
    source: redis-source
    description: some stuff
    commands:
      - ["PING"]